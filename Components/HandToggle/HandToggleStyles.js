import { StyleSheet } from 'react-native'

const HandToggleStyles = StyleSheet.create({
  toggleWrapper: {
    backgroundColor: 'purple',
    padding: '10%',
    borderRadius: 10
  },
  toggleText:{
    color:'gold'
  }
})

export default HandToggleStyles