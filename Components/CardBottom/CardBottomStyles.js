import {StyleSheet} from 'react-native'

const CardBottomStyles = StyleSheet.create({
  bottomWrapper:{
    display:'flex',
    flexDirection:'row',
    justifyContent:'space-between',
  }
})

export default CardBottomStyles